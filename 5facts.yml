- name: Facts
  hosts: localhost
  tasks:
    - name: Print Fact
      ansible.builtin.debug:

        var: ansible_lo

      #       error  {{ ansible_lo.ipv4.netmask }}
        #        "{{ ansible_lo['ipv4']['netmask']}}"
        #"{{ ansible_is_chroot }}"
        #"{{ ansible_interfaces[0] }}"
